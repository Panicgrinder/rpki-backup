{
  "is_template": true,
  "tasks": [
    {
      "id": "{{task_id}}",
      "title": "{{task_title}}",
      "description": "{{task_description}}",
      "status": "{{task_status}}",
      "assigned_to": "{{user_or_system}}",
      "created": "{{iso_timestamp}}",
      "completed": "{{iso_timestamp_completed}}"
    }
  ]
}