{
  "is_template": true,
  "locations": [
    {
      "id": "{{location_id}}",
      "name": "{{location_name}}",
      "sector": "{{sector_id}}",
      "type": "{{location_type}}",
      "description": "{{location_description}}",
      "coordinates": "{{coordinates}}",
      "linked_events": ["{{event_id}}"],
      "faction": "{{faction_id}}"
    }
    // Weitere Locations nach gleichem Muster
  ]
}